{
  "name": "vank",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run dict:download",
    "dev": "vite web --host 127.0.0.1",
    "prebuild": "npm run dict:download",
    "build": "tsc --noEmit && vite build web --base=./",
    "preview": "vite preview web --host 127.0.0.1",
    "pretest": "npm run dict:download",
    "test": "vitest",
    "pretest:run": "npm run dict:download",
    "test:run": "vitest run",
    "lint": "biome check --error-on-warnings .",
    "lint:fix": "biome check --write --error-on-warnings .",
    "typecheck": "tsc --noEmit",
    "knip": "knip",
    "ci": "npm run build:seed -- --output web/data/words.json && npm run lint && npm run typecheck && npm run test:run && npm run knip && npm run build",
    "prepare": "lefthook install",
    "dict:download": "node builder/scripts/download-dictionary.ts",
    "dict:publish": "node builder/scripts/publish-dictionaries.ts",
    "fetch": "node builder/src/cli.ts fetch",
    "normalize": "node builder/src/cli.ts normalize",
    "derive": "node builder/src/cli.ts derive",
    "enrich": "node builder/src/cli.ts enrich",
    "validate": "node builder/src/cli.ts validate",
    "dict:build": "node builder/scripts/build-dictionaries.ts",
    "build:seed": "node builder/src/cli.ts build --curated-only --no-ai --data-dir builder/data/seed"
  },
  "knip": {
    "entry": [
      "builder/scripts/discover.ts",
      "web/src/app.ts"
    ],
    "ignoreExportsUsedInFile": true
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@types/node": "26.4.1",
    "fake-indexeddb": "6.2.5",
    "knip": "6.34.0",
    "lefthook": "2.1.12",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@vercel/analytics": "2.0.1",
    "i18next": "26.4.1",
    "vankatum": "0.1.1"
  }
}
